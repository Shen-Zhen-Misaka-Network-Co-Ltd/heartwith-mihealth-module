<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/HeartwithLspTheme"
        android:usesCleartextTraffic="true">
        <provider
            android:name=".SettingsProvider"
            android:authorities="com.heartwith.mihealth.lsp.settings"
            android:exported="true" />

        <activity
            android:name=".SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".StatusReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.heartwith.mihealth.lsp.STATUS_CHANGED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
